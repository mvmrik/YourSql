# YourSQL — auto-login configuration
# Copy this file to .env and fill in your credentials.
# The .env file is ignored by git and never committed.

DB_HOST=localhost
DB_PORT=3306
DB_USERNAME=root
DB_PASSWORD=
DB_DATABASE=

# Optional: protect the app with a password before loading anything.
# Leave empty to disable (useful for local dev).
APP_PASSWORD=
